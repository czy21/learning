dependencyManagement {
    generatedPomCustomization {
        enabled = true
    }
    dependencies {
        dependency "com.learning:learning-infrastructure:${project.version}"
        dependency "com.learning:learning-spring-db:${project.version}"
        dependency "com.learning:learning-spring-web:${project.version}"
    }
}